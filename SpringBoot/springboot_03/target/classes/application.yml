
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tomcatdao
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
    #开启mp的日志（输出到控制台）
  main:
    banner-mode: off
#开启日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    banner: false
#修改端口
server:
  port: 80